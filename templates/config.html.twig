{% extends "generic_show_form.html.twig" %}
{% import 'components/form/fields_macros.html.twig' as fields %}

{% block form_fields %}
    {% if can_edit %}
        <input type="hidden" name="config_context" value="plugin:centreon">
        <div class="mb-3">
        <label class="form-label">Centreon username</label>
        <input type="text" class="form-control" name="centreon-username" placeholder="your username" value="{{ current_config['centreon-username'] }}">
        </div>
        <div class="mb-3">
        <label class="form-label">Centreon password</label>
        <input type="text" class="form-control" name="centreon-password" placeholder="your password" value="{{ current_config['centreon-password'] }}">
        </div>
    {% endif %}
{% endblock %}